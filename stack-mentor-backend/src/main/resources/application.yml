spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/stackmentor
    username: postgres
    password: postgres

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

  jpa:
    hibernate:
        ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: fabian.d.legair@gmail.com
    password: euha vynn yowc hhvl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true